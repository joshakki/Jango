version: '3.8'

ssssssss:
  mysql:
    image: mysql:8.0  # Use the latest MySQL version or specify a different one
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Akshay@123
      MYSQL_DATABASE: django
      MYSQL_USER: akshay
      MYSQL_PASSWORD: Akshay@123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - django_network

volumes:
  mysql_data:

networks:
  django_network:

